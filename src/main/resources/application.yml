spring:
  application:
    name: financeflow

  profiles:
    active: dev

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 12MB

  datasource:
      url: jdbc:postgresql://localhost:5432/financeflow?options=-c%20TimeZone=UTC
      username: postgres
      password: Kenwiw123
      driver-class-name: org.postgresql.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
jwt:
  secret: financeflow-secret-key-for-jwt-authentication-2026
  expiration: 86400000

server:
  port: 8080
